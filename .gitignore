# Dependencies are installed locally and should not be submitted as source code.
node_modules

# Next.js build output is generated by npm run build.
.next
out

# Local npm cache avoids global cache permission issues on this machine.
.npm-cache

# Local OS metadata is not part of the project source.
.DS_Store

# Supabase project keys live only on your machine.
.env*.local
!.env.local.example
